self-hosted-runner:
  labels:
    # Manual workflow_dispatch alternatives. Always-on Linux jobs use opaque
    # RunsOn Flex labels and macOS jobs use GitHub-hosted macos-latest.
    - warp-macos-15-arm64-6x
    - tart-canary
    - tart-dual
    - tart-small
